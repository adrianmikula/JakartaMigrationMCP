<?xml version="1.0"?>
<ruleset name="Jakarta Migration MCP Ruleset"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">
    
    <description>PMD ruleset for Jakarta Migration MCP project</description>
    
    <!-- Exclude patterns -->
    <exclude-pattern>.*/config/.*</exclude-pattern>
    <exclude-pattern>.*/dto/.*</exclude-pattern>
    <exclude-pattern>.*/entity/.*</exclude-pattern>
    <exclude-pattern>.*Application\.java</exclude-pattern>
    <exclude-pattern>.*Config\.java</exclude-pattern>
    
    <!-- Best Practices -->
    <rule ref="category/java/bestpractices.xml">
        <!-- Exclude some rules that may conflict with Spring Boot patterns -->
        <exclude name="JUnitTestsShouldIncludeAssert"/>
        <exclude name="JUnitAssertionsShouldIncludeMessage"/>
    </rule>
    
    <!-- Code Style -->
    <rule ref="category/java/codestyle.xml">
        <!-- Allow short variable names in some contexts -->
        <exclude name="ShortVariable"/>
        <exclude name="LongVariable"/>
    </rule>
    
    <!-- Design -->
    <rule ref="category/java/design.xml">
        <!-- Allow higher complexity for migration logic -->
        <exclude name="ExcessiveMethodLength"/>
        <exclude name="ExcessiveParameterList"/>
    </rule>
    
    <!-- Error Prone - includes high-priority rules (Priority 1) that will fail the build -->
    <rule ref="category/java/errorprone.xml">
        <!-- Exclude rules that may conflict with Lombok -->
        <exclude name="BeanMembersShouldSerialize"/>
    </rule>
    
    <!-- Performance -->
    <rule ref="category/java/performance.xml"/>
    
    <!-- Security -->
    <rule ref="category/java/security.xml"/>
    
</ruleset>

