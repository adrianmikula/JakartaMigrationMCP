

Move any tests which are in the root folder into the normal test folder structure, checking for duplication/overlap with existing tests, and updating them to use established test patterns in the project.

merge unique cases, but delete duplicated test cases.  ignore powershell scripts. organise tests by mosule and test type. rename tests to use the existing naming pattern of the workspace.