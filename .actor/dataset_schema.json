{
  "actorSpecification": 1,
  "fields": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "Placeholder field - this MCP server does not currently store data in Apify datasets"
      }
    }
  },
  "views": {
    "overview": {
      "title": "No Dataset Output",
      "description": "This MCP server does not store data in Apify datasets. Results are returned directly via the MCP protocol. See dataset_schema.json.example for a schema when dataset storage is implemented.",
      "transformation": {
        "fields": ["message"]
      },
      "display": {
        "component": "table",
        "properties": {
          "message": {
            "label": "Message",
            "format": "text"
          }
        }
      }
    }
  }
}

