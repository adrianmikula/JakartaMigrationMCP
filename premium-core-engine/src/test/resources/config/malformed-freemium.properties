# Malformed Configuration File for Testing Error Cases
# This file intentionally contains various configuration errors for testing

# Missing equals sign for URL
supabase.url https://malformed.supabase.co

# Empty anon key
supabase.anon.key=

# Invalid boolean value
supabase.analytics.enabled=maybe

# Negative batch size
supabase.analytics.batch.size=-5

# Invalid flush interval
supabase.analytics.flush.interval.seconds=not-a-number

# Duplicate property with different values
supabase.error.reporting.enabled=true
supabase.error.reporting.enabled=false

# Property with special characters
supabase.special.key=value=with=equals&symbols

# Very long value that might cause issues
supabase.very.long.key=This is an extremely long configuration value that exceeds reasonable limits and might cause parsing issues or memory problems in the configuration loading system especially when processed by certain property parsers that have size limitations or buffer constraints

# Unicode characters that might cause encoding issues
supabase.unicode.key=测试值🚀📊

# Invalid JSON-like structure
supabase.json.key={"invalid": "json", "missing": "quote}
