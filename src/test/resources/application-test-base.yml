# Base test configuration for all integration tests
# No payment or external licensing; default tier gives all features.

jakarta:
  migration:
    feature-flags:
      enabled: true
      default-tier: ENTERPRISE
