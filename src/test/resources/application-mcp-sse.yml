# Test configuration for mcp-sse profile
jakarta:
  migration:
    feature-flags:
      enabled: true
      default-tier: ENTERPRISE

spring:
  ai:
    mcp:
      server:
        transport: sse
        sse:
          port: 8080
          path: /mcp/sse
