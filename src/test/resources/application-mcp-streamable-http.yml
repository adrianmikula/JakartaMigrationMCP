# Test configuration for mcp-streamable-http profile
jakarta:
  migration:
    feature-flags:
      enabled: true
      default-tier: ENTERPRISE

spring:
  ai:
    mcp:
      server:
        transport: streamable-http
